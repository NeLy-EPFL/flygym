# Generated by scripts/dev/build_wasm_viewer_assets.py and regenerated on every
# model change (STL meshes + model_meta.json -- heavy, churning binaries). Kept
# out of the main branch; scripts/dev/push_doc_site.sh regenerates these and
# mkdocs bundles them into the site, so they are only ever published to the
# gh-pages (orphan, force-pushed) branch. Run the build script once to preview
# locally.
/assets/
